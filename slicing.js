const names = ['aaron', 'andrew', 'adrian', 'aston'];

const slicedNames = names.slice(1, 3); // This will create a new array containing elements from index 1 to 3 (excluding index 3)

console.log(slicedNames);
